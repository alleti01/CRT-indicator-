# Confirmation Lateness (Macro Signals)

**Status:** NOT_ELIGIBLE — N=1 macro signal.

See `PHASE77_MACRO_LATENESS.csv` if present.

Primary hypothesis is post-event repricing (T → T+60). Pre-event (T-15 → T) kept separate in subwindow tags.
