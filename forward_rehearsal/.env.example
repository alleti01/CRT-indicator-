# Forward rehearsal environment (copy to .env — never commit)
# DATABENTO_API_KEY=           # required for --use-databento live forward sessions
# PHASE74_WEBHOOK_SECRET=      # required for TradingView webhook auth
# PHASE74_KILL_SWITCH=0
