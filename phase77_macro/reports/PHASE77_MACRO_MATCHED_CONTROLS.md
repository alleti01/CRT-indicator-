# Matched Same-Time Controls

**Status:** NOT_ELIGIBLE — macro framework signals N=1 (< 30).

Matching requires SMD ≤ 0.10 on ATR, recent ranges, session progress, and activity covariates.

With only one macro-window signal, matched inference cannot run.

Same-time non-macro control universe: **2,639 bars** (15 sessions) for activation rate comparison — see `PHASE77_MACRO_LAYER_ACTIVATION.md`.
